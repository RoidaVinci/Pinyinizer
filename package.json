{
  "name": "clean-translate",
  "version": "0.3.0",
  "private": true,
  "description": "MV3 Chrome extension: in-place page translation and pinyin annotation with pluggable providers.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "bash scripts/check-syntax.sh"
  }
}
